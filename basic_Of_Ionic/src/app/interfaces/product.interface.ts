export interface Product {
    id: number;
    name: string;
    description: string;
    price: number;
    age?: number;   // using a ? this value is optional
  }

  //interface only for using checking datatype and structure 